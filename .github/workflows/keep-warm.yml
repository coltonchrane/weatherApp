name: Keep Warm
on:
  schedule:
    - cron: '*/15 * * * *' # Every 15 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Azure App
        run: |
          curl -I "https://weathercolt-abhnaggubzdfd4fa.canadacentral-01.azurewebsites.net/"
